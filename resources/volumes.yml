# Unity Catalog Volume definition
# Schemas (bronze/silver/gold) created manually or via SQL in the workspace

resources:
  volumes:
    raw_listings:
      catalog_name: job_market
      schema_name: bronze
      name: raw_listings
      volume_type: MANAGED
      comment: "Landing zone for raw JSON files uploaded by GitHub Actions scraper"
