# Local development dependencies (full stack)
ruff==0.5.7
black==24.8.0
pytest==8.3.2
pytest-cov==5.0.0
# Bot runtime (so the full test suite runs locally, not just lint)
python-telegram-bot[job-queue]==21.5
requests==2.32.3
duckdb==1.1.3
matplotlib==3.9.2
# Pipeline / dbt (self-hosted, DuckDB-based)
polars==1.4.1
pyarrow==17.0.0
dbt-core==1.8.7
dbt-duckdb==1.8.3
