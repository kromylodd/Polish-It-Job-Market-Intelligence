# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
.venv-pipeline/
.venv-test/
.testdeps/
venv/
env/
.conda/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# pytest
.pytest_cache/
htmlcov/
.coverage

# Local session notes
HANDOFF.md

# Bot user preferences (runtime, per-user)
telegram_bot/user_config.json
telegram_bot/analytics.db

# Bot premium runtime state (local caches / SQLite stores — disposable/private)
telegram_bot/serving.duckdb
telegram_bot/serving.duckdb.wal
telegram_bot/tracker.db
telegram_bot/payments.db
telegram_bot/alerts.db

# Pipeline database (runtime output — rebuilt from raw data by the pipeline)
pipeline.duckdb
pipeline.duckdb.wal

# Data files (don't commit raw scraped data)
data/
*.json
!scraper/tests/sample_raw_listing.json
!dbt/seeds/*.csv

# Personal career docs (do not publish)
CV_PLAN.txt
FULL_CAREER_PLAN.txt
LINKEDIN_*
restrictions.txt
