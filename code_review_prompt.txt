You are performing a full code review of a production Python project. Be thorough, critical, and specific. Reference exact file paths and line numbers where possible.

PROJECT: Polish IT Job Market Intelligence
A data pipeline (Polars + DuckDB + dbt) that scrapes job listings from justjoin.it, transforms them through a medallion architecture (bronze → silver → gold star schema), and serves them via a Telegram bot with paid analytics features (Telegram Stars). Runs on a GCP e2-micro VM.

TECH STACK:
- Python 3.12
- Polars 1.4 (data processing)
- DuckDB 1.1 (analytical database, serving cache)
- dbt-duckdb 1.8 (star schema modeling)
- python-telegram-bot 21.5 (bot framework, async)
- SQLite (payments.db, tracker.db, analytics.db — 3 separate databases)
- matplotlib (charts)
- GitHub Actions (CI/CD, daily scrape with SSH deploy to VM)
- systemd user service (hosting on GCP e2-micro VM)

RECENT CHANGES (last 2 days — focus review here, but check everything):
- Migrated pipeline from Databricks to self-hosted (Polars + DuckDB on VM)
- Added `/analytics reset` admin command (wipes events + filter_choices tables)
- Added `/givepremium` and `/revokepremium` admin commands
- Added user chat ID display in `/privacy`
- Rewrote `/mytracker` with pagination (10/page, status filter buttons)
- Added admin-only command menu (BotCommandScopeChat)
- Added referral / viral features to TODO (not yet implemented)
- Daily scrape workflow: added SSH secrets guard
- Pipeline: bronze_ingest.py, silver_clean.py, silver_tech_parse.py, run_pipeline.py
- Bot modules: bot.py (~2600 lines), payments.py, tracker.py, analytics.py, serving.py, notify.py, filters.py, reports.py, config_store.py

KEY FILES TO REVIEW:
- telegram_bot/bot.py (main bot logic — ~2600 lines, handles commands, callbacks, daily broadcast)
- telegram_bot/payments.py (subscription lifecycle, Telegram Stars, SQLite)
- telegram_bot/tracker.py (application tracker, SQLite)
- telegram_bot/analytics.py (anonymous usage stats, HMAC hashing, SQLite)
- telegram_bot/serving.py (DuckDB cache of gold marts for premium queries)
- telegram_bot/notify.py (daily broadcast logic)
- telegram_bot/filters.py (listing matching logic)
- telegram_bot/config_store.py (per-user config, JSON file + Volume mirror)
- pipeline/run_pipeline.py (orchestrator)
- pipeline/bronze_ingest.py (scraper JSON → DuckDB bronze)
- pipeline/silver_clean.py (data cleaning)
- pipeline/silver_tech_parse.py (technology extraction)
- .github/workflows/scrape.yml (daily scrape + SSH deploy)
- .github/workflows/ci.yml (lint + test)
- deploy/setup_vm.sh (VM provisioning script)

REVIEW SCOPE — check ALL of the following:

1. SECURITY ISSUES
   - Hardcoded secrets, tokens, or credentials in code (not .env)
   - SQL injection vulnerabilities (parameterized queries vs string formatting)
   - Input validation gaps (user-supplied data used unsafely)
   - Path traversal or file inclusion risks
   - Insecure deserialization
   - Secrets leaking into logs or error messages
   - Permissions issues on sensitive files
   - Bot command injection (can a user craft messages that break the bot?)
   - Telegram API token exposure
   - HMAC salt security (is ANALYTICS_SALT strong enough? is it actually secret?)
   - Admin chat ID bypass possibilities
   - Can users trigger admin commands by manipulating callback_data?
   - Are SQLite databases world-readable? Should they be chmod 600?

2. BUGS & LOGIC ERRORS
   - Race conditions (concurrent access to SQLite, DuckDB, JSON config files)
   - Edge cases not handled (empty inputs, null values, network failures)
   - Type mismatches (especially Polars Series vs Python list, DuckDB types)
   - Off-by-one errors in pagination
   - Memory leaks or unbounded growth (caches like _recent_listings, in-memory dicts)
   - Error handling that silently swallows important failures
   - Async/await issues (blocking calls on the event loop without asyncio.to_thread)
   - DuckDB connection conflicts (multiple writers, read-only vs read-write)
   - Thread safety of SQLite connections (check_same_thread, threading.Lock usage)
   - Tracker pagination: does page navigation break if data changes between pages?
   - Does `/givepremium forever` (36500 days) cause integer overflow anywhere?
   - What happens if the bot restarts mid-broadcast?

3. BAD HABITS & CODE SMELLS
   - bot.py is ~2600 lines — should it be split?
   - God functions (too long, too many responsibilities)
   - Copy-paste code that should be abstracted
   - Magic numbers without constants
   - Inconsistent naming conventions
   - Dead code / unreachable branches
   - Overly broad exception handling (bare `except Exception`)
   - Missing type hints where they'd add clarity
   - Functions with too many parameters
   - Mutable default arguments
   - Global state that makes testing harder
   - Using threading.local() vs proper connection pooling

4. UNUSED / DEAD CODE
   - Functions defined but never called
   - Imports that aren't used
   - Legacy Databricks code that may no longer be needed:
     * notebooks/ directory (was for Databricks jobs)
     * scraper/uploader.py (uploaded to Databricks Volume)
     * databricks.yml (DAB bundle config)
     * resources/ directory (Databricks job/dashboard configs)
   - Config options that have no effect
   - The bot still references Databricks for serving cache sync — is that still needed or is it pure DuckDB now?
   - Are there dead callback patterns registered but never triggered?

5. ARCHITECTURE & DESIGN
   - Circular dependencies between modules
   - Tight coupling that makes testing hard (bot.py imports everything)
   - Missing abstraction layers (e.g., no repository pattern for SQLite access)
   - Inconsistent patterns across tracker.py vs payments.py vs analytics.py (all use SQLite differently)
   - Database schema issues (missing indices, no constraints, no foreign keys)
   - DuckDB WAL mode and concurrent access correctness
   - Is the JSON config file a bottleneck? (read-modify-write under lock)
   - Three separate SQLite DBs — should they be consolidated?
   - systemd service configuration issues

6. CONCURRENCY & THREADING
   - bot.py uses asyncio (python-telegram-bot) + threading (SQLite, DuckDB)
   - Is the mix safe? Are there potential deadlocks?
   - Does the daily broadcast block other handlers?
   - Multiple thread-local SQLite connections (analytics.py) vs lock-per-call (tracker.py, payments.py) — which pattern is safer?
   - Config file writes: is _config_lock sufficient? What about Volume uploads?

7. PERFORMANCE
   - N+1 query patterns
   - Unnecessary full-table scans in SQLite
   - Loading large datasets into memory when streaming would work
   - Blocking I/O on the async event loop (any remaining non-to_thread calls?)
   - DuckDB queries that could use indices but don't
   - _recent_listings dict grows to 2000 entries — is that a problem?
   - Daily broadcast: does it hold the event loop for too long?

8. TESTING GAPS
   - Critical paths with no test coverage (what about bot.py handlers?)
   - Tests that don't actually assert meaningful behavior
   - Tests that depend on external state or ordering
   - Missing integration tests for the pipeline
   - Mocked dependencies that hide real bugs
   - No tests for: /givepremium, /revokepremium, /analytics reset, tracker pagination

9. DEPLOYMENT & OPERATIONS
   - Single points of failure (one VM, one process)
   - Missing health checks or monitoring (what if bot silently dies?)
   - Log levels inappropriate (sensitive data at INFO, important errors at DEBUG)
   - Backup strategy for SQLite databases (payments.db has billing data!)
   - What happens if the VM runs out of disk (DuckDB WAL growth, logs)
   - Graceful shutdown handling (SIGTERM from systemd)
   - Is the systemd restart policy correct? (Restart=always? Watchdog?)
   - GitHub Actions: are secrets rotated? Is the SSH key scoped properly?

FORMAT YOUR RESPONSE AS:

## Critical (must fix before going live with payments)
[issues that could cause data loss, security breaches, or service outages]

## High (should fix soon)
[bugs that affect correctness or reliability]

## Medium (improve when possible)
[code quality, maintainability, minor bugs]

## Low (nice to have)
[style, minor optimizations, suggestions]

## Dead Code to Remove
[list files/functions that serve no purpose — be specific about what's legacy Databricks vs still needed]

## Architecture Recommendations
[structural improvements for the next iteration]

For each issue, provide:
- File path and line number (or function name)
- What's wrong
- Why it matters
- Suggested fix (1-2 sentences)

Be brutally honest. This is a portfolio project that will be shown to recruiters — it should demonstrate engineering maturity, not just "it works." Don't sugarcoat. If something is fine, skip it. Focus on real problems.
