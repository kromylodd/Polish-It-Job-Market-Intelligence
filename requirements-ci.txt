# CI lint/test dependencies (lightweight, no JVM needed)
ruff==0.5.7
black==24.8.0
pytest==8.3.2
pytest-cov==5.0.0
requests==2.32.3
# Needed to actually run the serving-layer + reports tests (not just skip them)
duckdb==1.1.3
matplotlib==3.9.2
# Needed to import telegram_bot.bot in tests (test_bot_data.py)
python-telegram-bot[job-queue]==21.5
